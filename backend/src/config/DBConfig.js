import {
  DB_USERNAME,
  DB_PASSWORD,
  DB_DATABASE,
  DB_HOST,
  DB_DIALECT,
} from "./env.js";

export const username = DB_USERNAME;
export const password = DB_PASSWORD;
export const database = DB_DATABASE;
export const host = DB_HOST;
export const dialect = DB_DIALECT;
